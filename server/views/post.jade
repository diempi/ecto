extends layout

block title
    title= pageTitle + " > " + post.title

block content
    article
        header
            h2= post.title
            time( datatime=post.date )= post.date
        div.content
            != post.markdown

        footer
            p
                a( href="/" ) Retour
